<li>
	{{name}}

	<button class="devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devoured}}Make it again!{{else}}Devour it!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">Delete</button>
</li>